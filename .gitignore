# ── Symlink zu /opt/komms-data/.env (wird bei Install angelegt) ──────────────
server/.env

# ── Entwicklungsartefakte (lokale Testläufe) ─────────────────────────────────
server/nginx/certs/
server/authelia/configuration.yml
server/authelia/oidc-provider.yml
server/authelia/oidc.pem
server/matrix/homeserver.yaml.active
server/dnsmasq/dnsmasq.conf

# ── Entwicklungsartefakte ─────────────────────────────────────────────────────
.claude/
*.bak
*.swp
*.orig
__pycache__/
.DS_Store

# ── Veraltete Dateien ─────────────────────────────────────────────────────────
server/hmdm_init.sql
